#función lambda: son funciones anonimas, se usan cuando se quiere usar una función una unica vez (o pocas veces). Estas funciones no tienen nombre

#Función normal

def suma(a,b):
        return a + b
#Función lambda
lambda a, b: a + b 